apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: meu-app-deny-all
  namespace: default
spec:
  podSelector:
    matchLabels:
      app: meu-app
  policyTypes:
  - Ingress
  # Ao deixar 'ingress' vazio, você bloqueia TUDO por padrão